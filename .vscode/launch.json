{
  "configurations": [
    {
      "name": "LLDB: launch CMake project (Local)",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "terminal": "console",
      "preLaunchTask": "CMake: build"
    }
  ],
  "version": "0.2.0"
}
